<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Requests to pokeapi.co and sprites hosted on GitHub -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- F63 — lets PrefetchManager.isActiveNetworkMetered check whether the active connection is
         metered before a large prefetch, without needing any user-facing permission prompt (this
         is a normal, non-dangerous permission). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- B28 — PRIVACY_POLICY.md states favorites/teams/settings "stay on your device only" and are
         "deleted if you uninstall the app"; Android Auto Backup with allowBackup="true" (and the
         unedited backup_rules.xml/data_extraction_rules.xml templates, since removed) would have
         silently uploaded that SharedPreferences data to the user's Google Drive and let it
         survive an uninstall, contradicting the policy. -->
    <application
        android:name=".PikaDexApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PokeDex"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PokeDex">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
